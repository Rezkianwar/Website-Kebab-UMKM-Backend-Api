# Node.js
node_modules/  # Folder tempat semua dependensi Node.js diinstal
.env           # File variabel lingkungan yang berisi rahasia
npm-debug.log* # Log debug NPM
yarn-debug.log*
yarn-error.log*

# Logs
logs/          # Jika Anda memiliki folder untuk log aplikasi
*.log

# Dependency files
package-lock.json # Ini opsional, beberapa tim memilih untuk menyertakannya, tapi jika Anda mengelola monorepo, mungkin ada konflik. Untuk sub-proyek, biasanya disertakan. Namun, jika Anda punya package-lock.json di root monorepo, mungkin Anda ingin mengecualikan yang ini.
# Untuk saat ini, saya akan membiarkannya tidak dikomentari (tidak disembunyikan) karena ini spesifik untuk paket backend Anda.

# macOS specific files
.DS_Store

# VS Code specific files
.vscode/        # Folder konfigurasi VS Code (opsional, tergantung preferensi tim)
*.code-workspace

# Build artifacts
dist/           # Jika Anda memiliki proses build untuk backend Anda yang menghasilkan folder 'dist'
build/          # Atau jika menggunakan folder 'build'

# Production specific files (if any, e.g., temporary backups)
*.bak
*.tmp